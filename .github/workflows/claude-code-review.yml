name: Claude Code PR Review

on:
  pull_request:
    types: [opened, synchronize, reopened, labeled, unlabeled]

jobs:
  claude-review:
    runs-on: ubuntu-latest
    # PRがdraftでない場合のみ実行
    if: github.event.pull_request.draft == false
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          ref: ${{ github.event.pull_request.head.sha }}
      
      - name: Determine Claude model
        id: model
        run: |
          LABELS='${{ toJSON(github.event.pull_request.labels.*.name) }}'
          echo "Labels: $LABELS"
          
          # Opusを使用する条件
          if echo "$LABELS" | grep -qE "claude:opus|complex"; then
            echo "model=claude-opus-4-20250514" >> $GITHUB_OUTPUT
            echo "model_name=Claude Opus 4" >> $GITHUB_OUTPUT
            echo "model_display=🔴 Opus (高精度)" >> $GITHUB_OUTPUT
            echo "cost_estimate=$$$ (高コスト)" >> $GITHUB_OUTPUT
          # Sonnetを明示的に指定
          elif echo "$LABELS" | grep -q "claude:sonnet"; then
            echo "model=claude-sonnet-4-20250514" >> $GITHUB_OUTPUT
            echo "model_name=Claude Sonnet 4" >> $GITHUB_OUTPUT
            echo "model_display=🔵 Sonnet (標準)" >> $GITHUB_OUTPUT
            echo "cost_estimate=$$ (標準コスト)" >> $GITHUB_OUTPUT
          # デフォルトはSonnet
          else
            echo "model=claude-sonnet-4-20250514" >> $GITHUB_OUTPUT
            echo "model_name=Claude Sonnet 4" >> $GITHUB_OUTPUT
            echo "model_display=🔵 Sonnet (デフォルト)" >> $GITHUB_OUTPUT
            echo "cost_estimate=$$ (標準コスト)" >> $GITHUB_OUTPUT
          fi
      
      - name: Get changed files
        id: changed-files
        run: |
          # 変更されたファイルのリストを取得
          git diff --name-only origin/${{ github.base_ref }}...HEAD > changed_files.txt
          
          FILE_COUNT=$(wc -l < changed_files.txt)
          echo "file_count=$FILE_COUNT" >> $GITHUB_OUTPUT
          
          # 変更行数を取得
          STATS=$(git diff --shortstat origin/${{ github.base_ref }}...HEAD)
          echo "stats=$STATS" >> $GITHUB_OUTPUT
      
      - name: Post review start comment
        uses: actions/github-script@v7
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          script: |
            const modelDisplay = '${{ steps.model.outputs.model_display }}';
            const costEstimate = '${{ steps.model.outputs.cost_estimate }}';
            const fileCount = '${{ steps.changed-files.outputs.file_count }}';
            const stats = '${{ steps.changed-files.outputs.stats }}';
            
            const body = `## 🤖 Claude Code Review Started
            
            **Model:** ${modelDisplay}
            **Cost Estimate:** ${costEstimate}
            **Changed Files:** ${fileCount}
            **Stats:** ${stats}
            
            ---
            
            💡 **Tip:** ラベルを変更してモデルを切り替えられます
            - \`claude:opus\` または \`complex\` → Opus使用（複雑なタスク）
            - \`claude:sonnet\` → Sonnet使用（標準タスク）
            - ラベルなし → Sonnet使用（デフォルト）
            `;
            
            github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: body
            });
      
      - name: Setup Claude Code
        run: |
          # Claude Codeのインストール方法に応じて調整してください
          # 例: npm install -g @anthropic-ai/claude-code
          echo "Claude Code setup completed"
      
      - name: Run Claude Code Review
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
        run: |
          echo "Running Claude Code with model: ${{ steps.model.outputs.model_name }}"
          
          # PRの差分を取得
          git diff origin/${{ github.base_ref }}...HEAD > pr_diff.txt
          
          # Claude Codeを実行
          # 実際のコマンドは環境に応じて調整してください
          claude-code --model ${{ steps.model.outputs.model }} \
            --prompt "このプルリクエストをレビューしてください。以下の観点で確認をお願いします：
            1. コードの品質と可読性
            2. 潜在的なバグやエッジケース
            3. パフォーマンスの問題
            4. セキュリティの懸念
            5. ベストプラクティスへの準拠
            
            変更内容を確認し、改善提案があれば具体的なコード例とともに提示してください。" \
            < pr_diff.txt > review_result.txt
      
      - name: Post review results
        uses: actions/github-script@v7
        if: always()
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          script: |
            const fs = require('fs');
            let reviewResult = '';
            
            try {
              reviewResult = fs.readFileSync('review_result.txt', 'utf8');
            } catch (error) {
              reviewResult = 'レビュー結果の取得に失敗しました。';
            }
            
            const body = `## 📋 Claude Code Review Results
            
            **Model Used:** ${{ steps.model.outputs.model_display }}
            
            ${reviewResult}
            
            ---
            
            <details>
            <summary>ℹ️ レビュー情報</summary>
            
            - **Model:** ${{ steps.model.outputs.model_name }}
            - **Triggered by:** @${{ github.actor }}
            - **Commit:** ${{ github.event.pull_request.head.sha }}
            </details>
            `;
            
            github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: body
            });
      
      - name: Upload review artifacts
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: claude-review-${{ github.event.pull_request.number }}
          path: |
            review_result.txt
            changed_files.txt
            pr_diff.txt
